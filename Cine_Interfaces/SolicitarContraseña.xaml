<Window x:Class="Cine_Interfaces.SolicitarContraseña"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmar Contraseña" Height="200" Width="400" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Por favor, ingresa tu contraseña para confirmar:" 
                   FontSize="16" Margin="10" TextWrapping="Wrap" Grid.Row="0" />

        <PasswordBox x:Name="PasswordBoxContraseña" FontSize="14" 
                     Margin="10" Grid.Row="1" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="10">
            <Button Content="Confirmar" Width="100" Margin="5" Click="Confirmar_Click" />
            <Button Content="Cancelar" Width="100" Margin="5" Click="Cancelar_Click" />
        </StackPanel>
    </Grid>
</Window>
